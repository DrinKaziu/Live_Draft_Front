import React from 'react';


const PlayerList = (props) => {

    // const playersList = props.players.map(players => {
    //
    // })

  return(
    <div>

    </div>
  )
}

export default PlayerList;
